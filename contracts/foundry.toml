[profile.default]
src      = "src"
test     = "test"
out      = "out_forge"
libs     = ["lib"]
solc     = "0.8.24"

# Optimizer settings matching production
optimizer        = true
optimizer_runs   = 200

# Run fuzz tests with more iterations
[profile.default.fuzz]
runs = 256
